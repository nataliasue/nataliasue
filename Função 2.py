num = 2
def multiplica (b, h):
    return(b * h)
def divide (calcula_triangulo, num):
    return(calcula_triangulo / num)


base = float(input('Informe a base do triângulo: '))
altura = float(input('Informe a altura do triângulo: '))
calcula_triangulo = (base * altura) / num
print(calcula_triangulo)